

const Title = ({text}) => {
    return (
        <div>
            <h2 className="text-3xl lg:text-5xl font-semibold text-center uppercase">{text} </h2>
        </div>
    );
};

export default Title;